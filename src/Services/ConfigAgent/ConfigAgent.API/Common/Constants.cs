namespace ConfigAgent.API.Common;

public class Constants
{
    public class Credentials
    {
        public const string NotFoundException = "Credentials are not found";
        public const string ExistedCredentialsException = "Your credentials are already in the database, use them.";
    }
}